apiVersion: 1

providers:
  - name: 'Dashboards Korp'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      # Localização dos arquivos de dashboard dentro do container do Grafana
      path: /var/lib/grafana/dashboards