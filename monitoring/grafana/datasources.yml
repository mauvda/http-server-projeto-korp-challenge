apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: Prometheus
    # Usa o nome do container do Prometheus
    url: http://prometheus:9090 
    access: proxy
    isDefault: true